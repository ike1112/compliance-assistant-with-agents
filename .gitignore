.venv
.venv/
__pycache__/
.DS_Store
output/
analysis/
docs/*
# docs/ is human-only scratch EXCEPT the SLO contract, which the
# observability stack synthesizes alarms from and the infra test
# cross-checks — it must be a tracked, reproducible build input.
!docs/SLOs.md
.env
.env.*
!.env.example
infra/cdk.out/
.cdk.staging/
infra/cdk.context.json
.claude/review-gate.state.json
.claude/review-gate.last-outcome.json
.mutmut-cache
# coverage artifacts (test-run output, never tracked)
.coverage
**/.coverage
coverage.xml
